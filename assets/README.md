# Profile assets

Everything inside `assets/generated/` is produced by `npm run build` from
`profile.config.json`. Do not edit generated SVG files directly; your changes
will be overwritten the next time the generator runs.

Project logos live in `assets/logos/`. Set a project's `logoPath` in
`profile.config.json` to use one in its generated card.
